__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
*.db
*.sqlite
.DS_Store
*.log

# API Keys - NEVER commit
.env

# PDF Cache - Regenerate on each computer
.trace_cache/

# Note: PDFs in schematics/ are tracked by Git LFS (see .gitattributes)
# They will be stored in LFS, not regular git

# Keep these for syncing:
# trace_knowledge.json - Manual training
# schematic_registry.json - PDF indexes
